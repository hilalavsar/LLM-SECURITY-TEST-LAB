# --- Web katmani (L5/L6) ---
Flask==3.0.3
Jinja2==3.1.4

# --- Veritabani (Persistence) ---
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9

# --- Veri dogrulama (schemas) ---
pydantic==2.9.2

# --- Model runtime cagrisi (L2 adapters) ---
# Ollama ve OpenAI uyumlu API'ler standart kutuphaneyle (urllib) cagrilir.
# Optional: Google Gemini adapter (target model). Uninstalled = Gemini
# simply won't appear in the target dropdown; Ollama path unaffected.
google-generativeai==0.8.3

# --- Yardimcilar ---
python-dotenv==1.0.1
PyYAML==6.0.2

# --- Test ---
pytest==8.3.3
